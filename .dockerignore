# Dependencies (reinstalled during build)
node_modules

# Next.js build output
.next

# Local env (passed via docker-compose env_file)
.env.local

# SQLite dev database (data lives in the Docker volume)
prisma/*.db
prisma/*.db-journal

# Generated Prisma client (regenerated during build)
src/generated

# Dev tooling
.git
.gitignore
.eslintrc*
*.md
.claude

# macOS
.DS_Store
